# wiki/synthesis/ — cross-cutting essays and the evolving thesis

Pages that connect many sources into a single argument: tensions between principles, debates and where SVPG's position sits, how the model differs from adjacent frameworks, and **your own evolving point of view** as the wiki grows. These are the pages that make the wiki more than the sum of its sources.

Use the [`synthesis`](../../templates/synthesis.md) template.

Good answers to your own hard questions belong here — when a coaching session produces a genuinely new connection, file it as a synthesis page so it compounds instead of disappearing into chat history.
